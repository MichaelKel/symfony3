<article class="comment">
    <header>
        <p class="comment">
            {{ 'on' | trans | capitalize }} <time datetime="{{ comment.createdAt |date('c') }}"{{ comment.createdAt | date }}</time>
            {{ 'by' | trans }} {{ comment.userName }}
        </p>
    <p class="comment-body">{{ comment.body | nl2br }}</p>
    </header>
</article>