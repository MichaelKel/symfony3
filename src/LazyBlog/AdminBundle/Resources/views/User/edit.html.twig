{% extends 'AdminBundle::layout.html.twig' %}

{% block title %} Edit {{ entity.name }} {% endblock %}

{% block section %}
    <h1>Edit user {{ entity.name }}</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('lazyblog_admin_user_edit ') }}">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
