{% extends 'LazyBlogBundle::layout.html.twig' %}

{% block title %}
    {{ 'post.plural' | trans }}
{% endblock %}

{% block sidebar %}
    <aside>
        <h3>{{ 'post.latest' | trans }}</h3>

        {% for post in posts %}
            <p class="post-body"><a href="{{ path('lazyblog_lazyblog_post_show', { slug: post.slug }) }}">{{ post.title | trans | truncate(30) }} </a></p>
        {% endfor %}
    </aside>
{% endblock %}

{% block section %}
    {% for post in posts %}
        {{ include('LazyBlogBundle:Post:post.html.twig') }}
    {% endfor %}
{% endblock %}